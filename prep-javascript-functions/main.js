function addTwoNumbers(a, b) {
  return a + b;
}

var addTwoNumbersResult = addTwoNumbers(2, 2);

console.log('addTwoNumbers Exercise: ', addTwoNumbersResult);

function convertHoursToMinutes(h) {
  return h * (60);
}

var convertHoursToMinutesResult = convertHoursToMinutes(2);

console.log('convertHoursToMinutes Exercise: ', convertHoursToMinutesResult);

function getGreeting(g) {
  return '"Hello World!"';
}

var getGreetingResult = getGreeting('World');

console.log(getGreetingResult);

function addAndMultiplyBy5(m, n) {
  return (m + n) * (5);
}

var addAndMultiplyBy5Results = addAndMultiplyBy5(10, 5);

console.log(addAndMultiplyBy5Results);

function multiplyAndDivideBy5(d, e) {
  return (d * e) / (5);
}

var multiplyAndDivideResults = multiplyAndDivideBy5(35, 10);

console.log(multiplyAndDivideResults);

function subtractTwoNumbers(s, t) {
  return (s) - (t);
}

var subtractTwoNumbersResults = subtractTwoNumbers(22, 7);

console.log(subtractTwoNumbersResults);

function getCircleCircumference(r) {
  return (2) * (3.14159265) * (r);
}

var getCircleCircumferenceResult = getCircleCircumference(5);

console.log(getCircleCircumferenceResult);

function getFullName(t, y) {
  return (t) + ' ' + (y);
}

var getFullNameResults = getFullName('Jordan', 'Dawson');

console.log(getFullNameResults);

function cube(x) {
  return (x) * (x) * (x);
}

var cubeResults = cube(5);

console.log(cubeResults);
